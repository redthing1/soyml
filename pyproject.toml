[tool.poetry]
name = "soyml"
version = "0.3.0"
description = ""
authors = ["redthing1 <redthing1@alt.icu>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11,<3.15"
minlog = "^0.6.0"
numpy = "^2.4.0"
onnx = "^1.20.0"

onnxruntime = { version = "^1.23.2", optional = true }
wonnx = { version = "^0.5.0", optional = true }
torch = { version = "^2.9.1", optional = true }
torchinfo = { version = "1.8.0", optional = true }
ncnn = { version = "^1.0.20230517", optional = true }

[tool.poetry.extras]
ort = ["onnxruntime"]
wonnx = ["wonnx"]
ncnn = ["ncnn"]
torch = ["torch", "torchinfo"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
